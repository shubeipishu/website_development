# ============================================================
# 环境变量配置文件 (示例)
# 使用方式: 复制此文件为 .env 并根据需要修改
# ============================================================

# 环境类型: dev (开发) / prod (生产)
ENV_TYPE=dev

# Web 服务对外绑定地址与端口
# Windows 本地开发通常用 0.0.0.0:80 -> 浏览器访问 http://127.0.0.1
# (Linux 服务器上没有浏览器，只能通过 IP 访问)
# 云服务器请使用公网 IP 或域名访问，例如 http://<公网IP>:<端口>
WEB_BIND_IP=0.0.0.0
WEB_PORT=80
